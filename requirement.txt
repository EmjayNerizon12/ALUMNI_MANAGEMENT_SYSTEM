SQLite + Knex (current setup)

1) Install
  npm install

2) Run migrations
  npm run migrate:latest

3) Seed admin + questions
  npm run seed:run

4) Start server
  node app.js

Database file:
  data/alumni.sqlite  (preferred)
  data/alumni.sqlite3 (legacy supported)
